#!/bin/sh
# Refuses the push if the gate is red: typecheck, lint, test, build.
# Install: npm run install-hooks
set -e

if ! npm run --silent gate; then
  echo ""
  echo "  gate is red. fix it before pushing."
  echo "  run:  npm run gate"
  exit 1
fi
