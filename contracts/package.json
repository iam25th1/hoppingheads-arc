{
  "name": "hoppingheads-arc-contracts",
  "version": "0.1.0",
  "private": true,
  "description": "ArenaEscrow on Arc: the contract, its tests and the CLI harness that drives it on testnet.",
  "type": "module",
  "scripts": {
    "build": "arc-forge build",
    "test": "arc-forge test",
    "deploy:testnet": "node cli/deploy.mjs"
  },
  "dependencies": {
    "@openzeppelin/contracts": "5.6.1",
    "viem": "2.56.8"
  }
}
